import 'package:flutter/material.dart';

class DonationListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Donation List'),
      ),
      body: Center(
        child: Text('Donation List Screen'),
      ),
    );
  }
}
