import 'package:flutter/material.dart';

class OrganizationListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Organization List'),
      ),
      body: Center(
        child: Text('Organization List Screen'),
      ),
    );
  }
}
